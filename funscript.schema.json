{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Funscript",
  "description": "Schema for the Funscript format.",
  "type": "object",
  "properties": {
    "version": {
      "description": "Funscript version string. Supports 1.0 (single-axis), 1.1 (multi-axis with axes array), and 2.0 (multi-axis with channels object).",
      "type": "string",
      "enum": ["1.0", "1.1", "2.0"]
    },
    "inverted": {
      "description": "Indicates if the axes movement is inverted. Deprecated: Not widely supported.",
      "type": "boolean",
      "deprecated": true
    },
    "range": {
      "description": "Specifies the range of motion (0-100). Deprecated: Not widely supported.",
      "type": "integer",
      "minimum": 0,
      "maximum": 100,
      "deprecated": true
    },
    "actions": {
      "description": "Default actions for the primary axis. The primary axis is L0 (stroke), or inferred from file extension prefix (e.g. 'video.roll.funscript' → R1). In version 2.0, if both `actions` and `channels.stroke` are present, player should prefer `actions` for single-axis devices (e.g. Handy), and `channels.stroke` for multi-axis devices (e.g. SR6).",
      "type": "array",
      "items": { "$ref": "#/$defs/action" }
    },
    "axes": {
      "description": "Actions defined per axis. Used in version 1.1 for multi-axis scripts.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": { "$ref": "#/$defs/axisId" },
          "actions": {
            "description": "Actions for this specific axis.",
            "type": "array",
            "items": { "$ref": "#/$defs/action" }
          }
        },
        "required": ["id", "actions"]
      }
    },
    "channels": {
      "description": "Actions defined per channel. Used in version 2.0 for multi-axis scripts with named channels (e.g. stroke, roll, pitch, surge, sway, twist, suck).",
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "actions": {
            "description": "Actions for this specific channel.",
            "type": "array",
            "items": { "$ref": "#/$defs/action" }
          }
        },
        "required": ["actions"]
      }
    },
    "channel": {
      "description": "Human-readable channel name hint for the `actions` array.",
      "type": "string"
    },
    "metadata": {
      "$ref": "#/$defs/metadata"
    }
  },
  "anyOf": [
    { "required": ["actions"] },
    { "required": ["channels"] }
  ],

  "$defs": {
    "axisId": {
      "description": "Identifier for an axis (L0-L2, R0-R2, A0-A2, V0-V2). Further axes /[LRAV][3-9]/ are not used by any known device.",
      "type": "string",
      "enum": ["L0", "L1", "L2", "R0", "R1", "R2", "A0", "A1", "A2", "V0", "V1", "V2"]
    },
    "action": {
      "description": "A single action point with time and position.",
      "type": "object",
      "properties": {
        "pos": {
          "description": "Position value (0-100). Should be an integer, as some players don't support floats.",
          "type": "integer",
          "minimum": 0,
          "maximum": 100
        },
        "at": {
          "description": "Time in milliseconds. Should be an integer, as some players don't support floats.",
          "type": "integer",
          "minimum": 0
        }
      },
      "required": ["at", "pos"]
    },
    "timeSpan": {
      "description": "Time represented as a string, typically HH:MM:SS.ms or just seconds.",
      "type": "string",
      "pattern": "^(\\d+:)*\\d+(\\.\\d+)?$"
    },
    "chapter": {
      "description": "Defines a chapter or segment within the script.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Name of the chapter.",
          "type": "string"
        },
        "startTime": {
          "$ref": "#/$defs/timeSpan",
          "description": "Start time of the chapter."
        },
        "endTime": {
          "$ref": "#/$defs/timeSpan",
          "description": "End time of the chapter."
        }
      },
      "required": ["name", "startTime", "endTime"]
    },
    "bookmark": {
      "description": "A named time marker.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Name of the bookmark."
        },
        "time": {
          "$ref": "#/$defs/timeSpan",
          "description": "Time position of the bookmark."
        }
      },
      "required": ["name", "time"]
    },
    "metadata": {
      "description": "Optional metadata about the script.",
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "Title of the script or video."
        },
        "duration": {
          "type": "number",
          "description": "Duration of the video in seconds, with milliseconds precision.",
          "minimum": 0
        },
        "durationTime": {
          "$ref": "#/$defs/timeSpan",
          "description": "Human-readable duration hint (e.g. '00:03:00.017')."
        },
        "chapters": {
          "type": "array",
          "description": "List of chapters.",
          "items": { "$ref": "#/$defs/chapter" }
        },
        "bookmarks": {
          "type": "array",
          "description": "List of bookmarks.",
          "items": { "$ref": "#/$defs/bookmark" }
        },
        "creator": {
          "type": "string",
          "description": "Name of the script creator."
        },
        "description": {
          "type": "string",
          "description": "General description."
        },
        "license": {
          "type": "string",
          "description": "License information."
        },
        "notes": {
          "type": "string",
          "description": "Additional notes."
        },
        "performers": {
          "type": "array",
          "description": "Names of performers.",
          "items": { "type": "string" }
        },
        "tags": {
          "type": "array",
          "description": "Tags associated with the script. We recommend adding 'handy' tag for scripts supporting one.",
          "items": { "type": "string" }
        },
        "type": {
          "type": "string",
          "description": "Type of the script."
        },
        "topic_url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the topic or discussion thread."
        },
        "script_url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the script source."
        },
        "video_url": {
          "type": "string",
          "format": "uri",
          "description": "URL to the associated video."
        }
      }
    }
  }
}
